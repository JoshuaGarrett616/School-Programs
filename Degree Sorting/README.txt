This program takes a text file called data2, which displays graphs by having a node
in front and the numbers next to it being its adjacent nodes. Graphs are separated
by a blank line. The program makes use of linked lists and bucketsorting by 
displaying the sorted degrees in a file called out2.